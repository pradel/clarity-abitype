{
  "name": "clarity-abitype-root",
  "version": "0.0.0",
  "private": true,
  "description": "Monorepo for clarity-abitype",
  "scripts": {
    "ready": "vp fmt && vp lint && vp run -r build && vp run -r test",
    "prepare": "vp config",
    "changeset": "changeset",
    "version": "changeset version",
    "release": "turbo run build && cp README.md ./packages/clarity-abitype && changeset publish"
  },
  "devDependencies": {
    "@changesets/changelog-github": "0.5.2",
    "@changesets/cli": "2.29.8",
    "vite-plus": "0.1.15"
  },
  "packageManager": "pnpm@10.28.0"
}
