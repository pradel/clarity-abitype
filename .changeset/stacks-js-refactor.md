---
"clarity-abitype": minor
---

Create `typedCallReadOnlyFunction` and `typedMakeContractCall` function to provide type safe contract interaction as drop in replacements for the stacks.js `callReadOnlyFunction` and `makeContractCall` functions.
