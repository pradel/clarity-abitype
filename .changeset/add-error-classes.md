---
"clarity-abitype": minor
---

Add custom error classes (`BaseError`, `AbiFunctionNotFoundError`, `AbiArgumentMismatchError`, `ContractExecutionError`) with formatted diagnostics and cause chaining.
