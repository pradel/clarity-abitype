---
"clarity-abitype": minor
---

Create `typedCallContract` function to provide type safe contract interaction as drop in replacements for the `@stacks/connect` `request("stx_callContract")` method. Add new stacks-connect documentation.
